{
  "name": "@agenttrust/platform",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "dev": "node ../scripts/dev.js"
  },
  "devDependencies": {
    "prisma": "^6.6.0",
    "typescript": "^5.8.2"
  },
  "dependencies": {
    "@prisma/client": "^6.6.0"
  }
}
